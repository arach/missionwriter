node_modules/
dist/
.env
.env.local
.DS_Store
*.tsbuildinfo

# Mission run artifacts (per-run output dirs created by `mw run`)
.runs/
# OpenScout local state
.openscout/project.json

# Next.js runs viewer (HudsonKit app) — build + deps only; source is tracked
viewer/node_modules/
viewer/.next/
viewer/next-env.d.ts
viewer/*.tsbuildinfo
