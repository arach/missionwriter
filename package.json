{
  "name": "missionwriter",
  "version": "0.1.0",
  "description": "Lightweight CLI for running writing & review missions on top of @cursor/sdk.",
  "type": "module",
  "private": true,
  "bin": {
    "mw": "bin/mw.ts"
  },
  "scripts": {
    "mw": "bun bin/mw.ts",
    "serve": "bun bin/mw.ts serve"
  },
  "dependencies": {
    "@cursor/sdk": "^1.0.13",
    "@openscout/agent-sessions": "0.2.64",
    "yaml": "^2.6.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  }
}
