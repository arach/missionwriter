/** Shared constant — the theme script (server) reads/writes this key so the
 *  landing page paints in the Hudson dark theme pre-hydration (no flash). */
export const THEME_STORAGE_KEY = "missionwriter.theme";
